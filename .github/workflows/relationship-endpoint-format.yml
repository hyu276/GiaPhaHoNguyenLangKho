name: Temporary relationship endpoint formatter

on:
  push:
    branches:
      - feat/relationship-role-labels
    paths:
      - ".github/workflows/relationship-endpoint-format.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: feat/relationship-role-labels

      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Format endpoint labels
        run: npx prettier --write src/features/tree/components/relationship-editor-panel.tsx

      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/features/tree/components/relationship-editor-panel.tsx
          git commit -m "style(relationship): Format endpoint labels"
          git push origin HEAD:feat/relationship-role-labels
