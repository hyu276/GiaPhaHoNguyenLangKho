# Supabase migrations

Every persistent schema, constraint, index, function, trigger, and RLS policy change must be represented by a versioned migration in this directory.

Do not store private production family data here. Seed and fixture data must be synthetic and safe for repository use.
